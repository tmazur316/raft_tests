package main

func main() {
	/*nodeAddr := flag.String("nAddr", "127.0.0.1:5000", "Address of a node" )
	flag.Parse()
	fmt.Printf("%s\n", *nodeAddr)
	config := raft.DefaultConfig()
	fmt.Print(*config)*/

	//rft := raft.NewRaft()
}
